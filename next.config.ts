import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['192.168.1.7', 'saturnina-posthexaplar-shawana.ngrok-free.dev'],
};

export default nextConfig;
